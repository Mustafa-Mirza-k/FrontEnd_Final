'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

require('react');
var styled = require('styled-components');
var styled__default = _interopDefault(styled);
require('../index-1781dba3.js');
require('../Svg-5494338c.js');
var Close = require('../Close-e5f32b37.js');

var CloseIcon = styled__default(Close.CloseIcon).attrs(({ setStatus, iconSize = "large", onClick }) => ({
    iconSize,
    onClick: onClick || (() => { if (setStatus) {
        setStatus(false);
    } })
})) `
    opacity: .5;

    &:hover {
        opacity: 1;
    }
`;

module.exports = CloseIcon;
