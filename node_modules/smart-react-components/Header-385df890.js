'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var styled = require('styled-components');
var styled__default = _interopDefault(styled);
var index$1 = require('./index-1781dba3.js');

var HeaderElement = styled__default.header.attrs(index$1.attrs)(index$1.css);

var ModalHeader = styled__default(HeaderElement).attrs(({ bottomBorder = true, flex = "0 0 auto", display = "flex", justifyContent = "space-between", alignItems = "center" }) => ({ bottomBorder, flex, display, justifyContent, alignItems }))(({ theme, bottomBorder }) => `
    box-sizing: border-box;
    padding: ${theme.src.modal.modalContentPadding.y} ${theme.src.modal.modalContentPadding.x};

    ${bottomBorder ? `
        border-bottom: solid 1px ${theme.src.modal.modalContentBorderColor};
    ` : ""}
`);

exports.ModalHeader = ModalHeader;
