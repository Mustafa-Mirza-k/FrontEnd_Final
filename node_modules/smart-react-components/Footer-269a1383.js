'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var styled = require('styled-components');
var styled__default = _interopDefault(styled);
var index$1 = require('./index-1781dba3.js');

var FooterElement = styled__default.footer.attrs(index$1.attrs)(index$1.css);

var ModalFooter = styled__default(FooterElement).attrs(({ flex = "0 0 auto" }) => ({ flex }))(({ theme, topBorder }) => `
    display: flex;
    justify-content: flex-end;
    align-items: center;
    box-sizing: border-box;
    padding: ${theme.src.modal.modalFooterPadding.y} ${theme.src.modal.modalFooterPadding.x};
    
    > * {
        flex: 0 0 auto;
        margin: ${theme.src.modal.modalFooterContentMargin.y} ${theme.src.modal.modalFooterContentMargin.x};
    }

    ${topBorder ? `
        border-top: solid 1px ${theme.src.modal.modalContentBorderColor};
    ` : ""}
`);

exports.ModalFooter = ModalFooter;
