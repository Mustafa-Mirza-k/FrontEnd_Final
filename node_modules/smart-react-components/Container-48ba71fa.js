'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var styled = require('styled-components');
var styled__default = _interopDefault(styled);
var Div = require('./Div-d2bf7bfb.js');

var BadgeContainer = styled__default(Div.Div)(({ theme, space }) => `
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    margin-left: -${theme.src.badge.margin.x};
    margin-right: -${theme.src.badge.margin.x};

    ${space
    ? `
        margin-top: -${theme.src.badge.margin.y};
        margin-bottom: -${theme.src.badge.margin.y};
    ` : ""}

    > .src-badge {
        flex: 0 1 auto;
    }
`);

exports.BadgeContainer = BadgeContainer;
