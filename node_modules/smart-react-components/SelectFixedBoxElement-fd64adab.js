'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var styled = require('styled-components');
var styled__default = _interopDefault(styled);
var Div = require('./Div-d2bf7bfb.js');

const customKeys = {
    size: (v, t) => `
        > div {
            padding: ${t.form.optionMargin[v].y} 0;

            > section {
                padding: 0 ${t.form.optionPadding[v]};
                margin: ${t.form.optionMargin[v].y} ${t.form.optionMargin[v].x};
            }
        }
    `
};
var SelectFixedBoxElement = styled__default(Div.Div).attrs({ customKeys })(({ theme, type, shape }) => `
    overflow: auto;
    -webkit-overflow-scrolling: touch;
    border-radius: ${theme.src.form.inputRadius[shape]};
    background: ${theme.src.type[type].form.inputBackground};
    user-select: none;
`);

exports.SelectFixedBoxElement = SelectFixedBoxElement;
