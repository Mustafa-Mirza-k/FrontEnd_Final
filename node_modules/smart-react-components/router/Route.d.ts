import React from "react";
import { RouteProps } from "../types/router";
export interface Props extends RouteProps {
    children?: never;
    switchNotRendered?: boolean;
}
declare const Route: React.FC<Props>;
export default Route;
