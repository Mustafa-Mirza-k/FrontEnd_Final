import React from "react";
import { Path } from "../types/router";
interface Props {
    to: string;
    path?: Path;
    exact?: boolean;
    checkActive?: boolean;
}
interface Return {
    click: (e: React.FormEvent<HTMLAnchorElement>) => void;
    active: boolean;
    activating: boolean;
}
declare const _default: ({ to, path, exact, checkActive }: Props) => Return;
export default _default;
