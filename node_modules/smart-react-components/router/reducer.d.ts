import { ReducerState } from "../types/router";
export declare const getInitialState: (url?: string) => ReducerState;
declare const reducer: (state: ReducerState, action: any) => ReducerState;
export default reducer;
